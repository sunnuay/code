syntax = "proto3";

package api;
option go_package = "./api";

message Request {
  string text = 1;
}

message Response {
  string text = 1;
}

service Core {
  rpc Handle (Request) returns (Response);
}
